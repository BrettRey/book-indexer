# CLAUDE.md

Book indexing tool for LaTeX academic books. Primary use: HPC book. Secondary: share with LangSci Press.

## Role: Editor

Deep technical work welcome in this project.

## LLM Assist

Use `python -m book_indexer.cli assist` for lexicon normalization and cross-refs.
Gemini CLI is supported via `scripts/gemini_llm.py`.

## Multi-Agent Dispatch

Before dispatching multiple agents, ask Brett which model(s) and whether redundant outputs are wanted.
